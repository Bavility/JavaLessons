// Напишите программу, которая выводит таблицу умножения для чисел от 1 до 10. Попробуйте реализовать это с использованием вложенных циклов.

public class Main {
public static void main(String[] args) {

    // Вложенные циклы для создания таблицы умножения
    for (int i = 1; i <= 10; i++) {
    for (int j = 1; j <= 10; j++) {
      
    // Выводим результат
    System.out.printf("%4d", i * j);
    }
    System.out.println(); // Переход на новую строку после каждой строки таблицы
}
}
}

/*
При запуске программы вы увидите таблицу умножения для чисел от 1 до 10.
Каждый элемент таблицы будет выровнен по ширине, чтобы таблица выглядела аккуратно.
 */
